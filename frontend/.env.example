# Optional: during `npm run dev`, requests to /api/* are proxied to the Express
# backend at http://localhost:3001 (see vite.config.ts), so this can be left unset.
# Set it explicitly when serving a production build against a non-local backend:
# VITE_TOKEN_ENDPOINT=http://localhost:3001/getToken
